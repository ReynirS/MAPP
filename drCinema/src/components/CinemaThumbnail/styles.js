import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  view: {
    margin: 10,
    padding: 10,
    borderWidth: 2,
    borderColor: 'black',
    alignItems: 'center',
  }
});
